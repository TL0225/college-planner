Onboarding scrape audit notes (execution artifacts; plan file not modified)
Date: 2026-05-06

BROWSER — catalogs.buffalo.edu
- Loaded https://catalogs.buffalo.edu/ : Modern Campus gateway with catalog combobox (Undergraduate, Graduate, Law, Medical, Dental, etc.). No WAF interstitial on this check.
- robots.txt (curl https://catalogs.buffalo.edu/robots.txt): shared Modern Campus template; User-agent * Disallow includes /search_advanced.php, /ajax/, /portfolio*.php; crawl-delay 120 (app clamps polite spacing for interactive sync).

IMPLEMENTATION SUMMARY
- ModernCampusEngine.fetchHTML: register catalog host for WK fallback allowlist; robots path guard; polite delay; WAF heuristics + WKWebView rendered HTML fallback (outerHTML) with dedicated AsyncSemaphore(2).
- CatalogRenderedHTMLFetcher: MainActor WKWebView returning HTML for SwiftSoup.
- CatalogOriginRobotsThrottle: cached robots fetch, merged disallows, crawl-delay with caps (user-facing vs background).
- AssistantWebFetchPolicy: catalog scrape origin host set for rendered fetch only.
- UniversalCatalogScraper: CatalogProgramScrapeDepth .topologyOnly skips course cache + bulk requirements; .fullRequirements unchanged.
- CatalogBackgroundSyncRunner: depth .light now passes .topologyOnly to scraper (was only messaging before).
- OnboardingRootView: program scrape uses .topologyOnly; JIT requirements via existing persistence coordinator.refreshProgramRequirementsIfNeeded + Major views.
- CatalogEmbeddingRuntime: splits long text into ~2400 char sub-chunks, mean+L2 normalize for multi-chunk embeds (more MLXTaskQueue handoffs).
- MLXTaskQueue: error log when backlogExceeded.
- LocalLLMRunner / ModelManager: mlx-swift Examples baseline documentation.
- MajorDetailsView / MajorMinorDetailsView: skeleton copy "Fetching catalog requirements…".

BUILD
- xcodebuild -scheme College -destination platform=macOS : exit 0.

E2E / HANDS-ON
- Full UB onboarding + local store + Assistant walkthrough must be run on a developer machine with the app and data container; not automated in this session.
