import SwiftUI

enum ToolbarMetrics {
    static let titleFont = Font.system(size: 16, weight: .semibold)
    static let controlFont = Font.system(size: 13, weight: .semibold)
    static let iconFont = Font.system(size: 14, weight: .semibold)
    static let itemHorizontalPadding: CGFloat = 8
    static let itemSpacing: CGFloat = 12
}
